# AI Research Copilot — 面试讲解稿

> 使用方式：先看「30 秒电梯陈述」建立主线，再逐条消化 Q&A。**讲的时候用自己的话，别背稿。** 每条 Q&A 后面标注了"答什么"和"为什么这样答"。

---

## 30 秒电梯陈述

> 我做了一个单篇论文的证据绑定阅读助手。用户上传 PDF 后，系统解析章节和页码、向量检索并在生成前注入有预算的论文证据，AI 据此输出带【来源章节 + 页码 + 原文引用】的摘要和问答。核心不是再做一个论文聊天助手，而是让论文事实、领域背景和模型推断有清晰边界。

---

## Q1：为什么不用普通 ChatGPT / 豆包？

**答**：普通 LLM 会混淆三个世界——**论文内容、模型训练知识、后续领域发展**。我实测问"Transformer 论文的不足"，它把 Pre-LN（2021 年后才流行的结构）说成论文用了；还把 KV Cache、RoPE 这些后续技术混进来。科研用户最怕"历史倒置"。所以我的系统让模型**只基于检索到的论文文本作答**，并且每个结论强制带来源。

**为什么这样答**：这直接点出项目的差异化——不是"会总结"，而是"可信"。这是 AI 应用岗位最关心的 RAG 质量问题（幻觉）。

## Q2：RAG 怎么设计？为什么按章节切，不按固定窗口切？

**答**：论文有天然结构（Abstract / Method / Experiment / Conclusion）。我先按结构识别章节，再在章节内按长度切块；每个块带自己的页码范围。这样既不把整篇固定窗口盲切，也不会把一个很长章节整体塞进上下文。

1. **语义与覆盖兼顾**：先保留语义检索的核心块，再用结构化补齐补足缺失章节。
2. **可溯源**：metadata 带章节名 + chunk 实际页码，检索命中后模型能说"这来自 Method 第 4 页"。

**为什么这样答**：展示你理解"切块策略直接影响 RAG 质量"，这是面试官想听的工程判断。

## Q3：怎么降低幻觉？你的回答凭什么可信？

**答**：三层手段，从数据到 prompt 到输出结构：
1. **数据层（页码溯源）**：PyMuPDF 逐行记录页码，chunk 携带 page_start/page_end，每个证据块标到【来源: Method | 第 1-2 页】。
2. **Prompt 层（证据铁律）**：检索文本是唯一事实来源；一级事实（贡献/实验数据/结论）必须带【原文引用】；论文没提的必须说"论文中未找到相关描述"。
3. **结构层（三类分层）**：回答研究 idea 类问题时强制分——A1 论文明确说的 / A2 从设计推导的 / B 后续研究（标"非论文内容"）/ C 我的推断。严禁把 B/C/A2 伪装成论文原文。

**为什么这样答**：这是项目的技术护城河，三层设计分别对应数据、约束、输出三个层面，体现出系统思维。面试官追问"为什么 prompt 管得住"时，可以补一句：对名篇（如 Transformer）模型知识很强，硬约束 + 原文引用要求才能压住它补全的冲动。

## Q4：为什么不用整篇 PDF 直接输入？

**答**：对一次性的短论文精读，整篇输入是合理选择；V1 的目标不同，是有来源的多轮问答。它每轮只注入最多 8 个、总计 6000 字符的证据块，证据不写进历史，下一轮按需重取。因此不会随着追问把论文原文线性重复发送，也能明确告诉用户"这个结论来自哪一节、哪一页"。

**为什么这样答**：展示你懂 token 成本模型和 RAG 的动机，而不只是"别人都这么做"。

## Q5：为什么用 OSS？直接传 PDF 不行吗？

**答**：直接传更简单，OSS 也不省 token（PDF 从不进 LLM）、还多一跳延迟。我留 OSS 有两个原因：
1. **V2 铺路**：多论文知识库需要持久化原始 PDF 作为数据源。
2. **云集成**：阿里云 OSS + STS 临时凭证 + 前端直传是云原生的标准做法。
**（如果你被问"那你为什么不现在简化？"）**：打磨期改一个能工作的上传链路有回归风险，留到需要时再收敛。

**为什么这样答**：诚实 + 有取舍。面试官欣赏"知道每个技术选择的代价"的人，而不是无脑堆云服务。主动说出 OSS 的缺点，比被发现更得分。

## Q6：遇到的最大坑？

**答**：两个。
1. **证据跨轮累积**：早期 Agent 工具返回的大段论文文本会进入 checkpoint，追问越多 token 越高。修法是后端预检索并把证据仅注入当前动态提示词，同时清理旧 ToolMessage。
2. **覆盖上传串论文**：只删向量不删对话时，旧论文的文字回答仍会影响新论文。修法是在新 PDF 成功解析后同时清空该 thread 的 checkpoint 历史和旧向量集合。

**为什么这样答**：这两个坑分别体现"API 边界排错"和"前后端契约"的工程能力，比说"我调通了"有说服力。

## Q7：项目不足 / 未来规划？

**答**：三点边界——页码是 PDF 物理页码；章节检测不能保证适配所有 PDF 排版；当前是一会话一篇论文的本地 SQLite 工作流。向量检索加了 MMR，但没有 cross-encoder 精排。

下一步不应为了复杂而堆多 Agent。更有价值的方向是论文、代码、数据和实验结果的复现与比较闭环，尤其适合时间序列研究。

**为什么这样答**：主动暴露不足 + 给出解决路径，展示成长性和对版本的规划感。

---

## 面试官可能追问的细节

**Q：三类分层里 A2 和 C 的区别？**
A2 是从论文设计**推导**出的潜在问题，依据仍在论文内；C 是纯模型推断，可以完全超出论文。两者都要标注来源性质，但 A2 更接近论文、C 更自由。

**Q：证据块里"第 1-2 页"怎么来的？**
解析时逐行记录页码；切块时把每个 chunk 覆盖的实际页码范围写进 metadata，检索时带出来格式化。

**Q：为什么不把更多文本直接塞给模型？**
当前会从较大的候选池中用 MMR 选块，并限制最多 8 个证据块；若真实评测显示正确段落常落在候选集中却没有进入最终证据，再考虑 cross-encoder 精排。

**Q：ChromaDB 用的什么距离度量？**
默认 L2。要余弦得显式声明 `hnsw:space: cosine`——这是很多人踩的坑，因为 embedding 库默认不是你以为的度量。

---

## 一句话总结（收尾用）

> 这个项目证明的不只是"我会调 Agent"，而是**我能为一个真实场景（科研阅读的可信度）设计数据链路、约束模型行为、并用版本规划推进**。
