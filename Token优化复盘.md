# Token 爆炸治理复盘 —— Research Copilot 优化学习文档

> 这份文档讲清楚一件事：**早期版本为什么会"token 爆炸"（又贵又慢还容易超限），后来分别用什么方法解决，效果如何，以及还剩什么坑。**
> 建议配合代码阅读，文中都标了 `文件名:行号`。

---

## 一、先理解：什么是"token 爆炸"，钱到底烧在哪？

大模型 API 按 token 计费，而且**每次请求都要把"对话历史 + 系统提示 + 新输入"整个重新发一遍**。在「RAG + Agent + 多轮对话」这套架构里，有三个地方会让 token 像滚雪球一样越滚越大：

1. **Agent 工具循环**：Agent 不是一次答完，而是"想 → 调工具 → 看结果 → 再想 → 再调工具 → 答"。每调一次检索工具，返回的一大段论文文本都会进入上下文，下一轮思考时又要把它重新发给模型。
2. **多轮历史累积**：LangGraph 用 SqliteSaver 存了全部历史消息。第二轮对话时，**第一轮里检索回来的大段论文文本（ToolMessage）还躺在历史里，会被原样再发一次**；第十轮时，前九轮的所有检索结果全部重发。这是爆炸的**主因**——token 随对话轮数线性增长。
3. **不该检索的也检索**：用户问"什么是 FlashAttention"这种通用领域知识，Agent 照样去论文库里捞一堆片段塞进上下文，纯属白烧。

一句话总结根因：**检索回来的大段文本，被"反复地、无节制地、跨轮次地"发给了模型。**

---

## 二、改进总览

| # | 改进 | 解决的问题 | 核心技术/方法 | 代码位置 |
|---|------|-----------|--------------|---------|
| 1 | 查询路由 A1/A2/B/C | 不该检索的问题白烧 token | 生成前一次廉价 LLM 分类 | `query_router.py` |
| 2 | Python 侧预检索 + 证据注入 | Agent 多轮工具循环反复重发 | 把"Agent 自主调工具"改成"后端先查好、塞进提示词" | `app.py:52,88` |
| 3 | 清理历史里的工具消息 | **跨轮次重发（爆炸主因）** | `RemoveMessage` 删除 ToolMessage / tool_calls | `app.py:172-183` |
| 4 | MMR 重排 + chunk 去重 | 多次检索返回重复片段 | 最大边际相关算法 + exclude_ids | `rag_store.py:52-67,96` |
| 5 | 证据预算控制 | 单次检索返回太多 | 截断 600 字、每查询 2 条、固定 4 个摘要查询 | `app.py:44-69` |
| 6 | recursion_limit = 15 | Agent 工具死循环 | LangGraph 递归上限 | `app.py:170` |
| 7 | plus → flash 模型 | 单价高、速度慢 | 换轻量模型 | `config.py:11` |
| 8 | embedding 分批 + 错误透出 | 超 20 条批次直接 400 报错 | 分批 + 按 index 排序 | `rag_store.py:20-38` |
| 9 | 重新上传先删旧集合 | 多篇论文 chunk 混在一起污染检索 | 上传即 `delete_collection` | `app.py:139` |
| 10 | SSE 检索进度提示 | 烧 token 时界面"卡死无反应" | 工具调用事件推送状态 | `app.py:197-207` |
| 11 | 证据铁律 + 三类分层 | 幻觉、把后续技术说成论文内容 | Prompt 强约束 + 溯源引用 | `prompts.py` |

---

## 三、逐项详解

### 改进 1：查询路由（Query Router）—— 先判断"要不要查论文"

**问题**：用户的问题分两种，一种必须查论文（"这篇论文用了什么数据集？"），一种根本不用（"什么是注意力机制？"）。早期 Agent 对所有问题都调检索工具，后者白白把论文片段塞进上下文。

**做法**：在正式让 Agent 生成回答**之前**，先用一次**极其便宜**的 LLM 调用把问题分成四类（`query_router.py:9-16`）：

- **A1 论文事实**：问论文里明确写的（方法/实验/结论）→ 要检索
- **A2 论文不足**：问论文局限/改进空间 → 检索 limitation、future work
- **B 领域知识**：通用知识，与这篇论文无关 → **不检索**，直接让模型用自身知识回答
- **C 创新推理**：要结合论文提新想法 → 检索论文细节 + 结合领域知识

这次分类调用刻意压到 `max_tokens=50, temperature=0`（`query_router.py:52`），输出只有一个 `{"category": "A1"}`，成本几乎可忽略。分类失败时**保守回退 A1**（宁可多检索也不漏检索，`query_router.py:41,60`）。

**技术要点**：这是一种"**用小模型/廉价调用做决策，省下大模型的盲目执行**"的思路，在 Agent 系统里叫路由（routing）/ 分流。

---

### 改进 2：预检索 + 证据注入 —— 干掉 Agent 的多轮工具循环

**问题**：早期让 Agent 自己决定调 `retrieve_paper` 工具。它为了写结构化摘要，往往要调 3 次工具（Abstract、Method、Experiment 各一次），每次工具返回的大段文本都进入上下文，下一轮思考时整体重发 —— 光是"写一篇摘要"就在工具循环里烧了好几轮 token。

**做法**：把检索动作从"Agent 在循环里自主调用"改成"**后端 Python 在调用 Agent 之前就查好**"：

- `build_evidence()`（`app.py:52-69`）：后端直接调 `rag.search()`，把命中的片段格式化成 `【证据 #1 | 来源: Method | 第 4-9 页】...`，拼成一段证据文本。
- 上传论文时用固定的 4 个查询一次性覆盖摘要所需章节（`SUMMARY_QUERIES`，`app.py:44-49`）。
- 然后把证据**直接写进给 Agent 的用户消息里**，并明确指示"证据已给你，**不要调用检索工具**，直接基于证据回答"（`app.py:157-163`）。

**效果**：Agent 从"想→查→想→查→答"的多轮往返，变成"看证据→一次答完"的单轮。工具循环没有了，反复重发也就没有了。

> 这是本次优化里**架构思路上最大的转变**：不是让 Agent 更聪明地用工具，而是在确定性高的环节（摘要、论文事实问答）干脆替它把工具用掉，只把"综合与表达"交给模型。

---

### 改进 3：清理历史里的工具消息 —— 堵住跨轮次爆炸（最关键）

**问题**：这是 token 爆炸的**主因**。即使单轮优化了，多轮对话时 SqliteSaver 会把历史里所有消息重发给模型，而历史中 `ToolMessage`（检索回来的论文原文）体量最大。聊得越久，重发的旧检索结果越多，呈线性膨胀。

**做法**：每次请求开始、调用 Agent 之前，先把历史里的"工具相关消息"删掉（`app.py:172-183`）：

```python
to_remove = [
    RemoveMessage(id=m.id)
    for m in state.values.get("messages", [])
    if isinstance(m, ToolMessage) or (isinstance(m, AIMessage) and m.tool_calls)
]
if to_remove:
    agent.update_state(config, {"messages": to_remove})
```

- 删除两类：`ToolMessage`（工具返回的大段论文文本）和**带 tool_calls 的 AIMessage**（模型"我要调工具"的那条指令，不删的话会和工具结果对不上）。
- 保留：普通的用户消息和 AI 的文字回答 —— 所以对话的"可读性/连贯性"还在，只是把又大又可重新获取的检索原料清掉了。

**为什么安全**：论文证据随时可以用 RAG 重新检索，不需要常驻上下文；而用户问过什么、AI 答过什么（纯文本）体量小，保留即可维持多轮连贯。

**效果**：每轮发送量从"随轮数增长"变成"**基本恒定**"（系统提示 + 当前证据 + 最近的问答文字）。

---

### 改进 4：MMR 重排 + chunk 级去重 —— 同样的内容不发第二遍

**问题**：向量检索默认按相似度返回 top-k，但论文里同一章节的段落往往高度相似，导致多次检索**反复命中同一批 chunk**，token 花了、信息量没增加。

**做法（两层去重）**：

1. **会话级 chunk 记忆**（`agent_setup.py:17,30-36`）：用 `_seen_chunks` 记录本会话已经发给模型的 chunk id，检索时通过 `exclude_ids` 排除；每轮新请求开始时 `reset_retrieval_memory()` 清零（`agent_setup.py:50-53`），保证新一轮能从头检索。
2. **MMR（最大边际相关）重排**（`rag_store.py:52-67`）：先召回 20 个候选（扩大召回面，`pool=20`），再用 MMR 公式在"与问题相关"和"与已选内容不重复"之间折中挑 k 个：

   `score = λ × 相关度 − (1−λ) × 与已选的重复度`（λ=0.7，偏相关但兼顾多样性）

**技术要点**：MMR 是 RAG 里标准的"多样性检索"手段，解决"top-k 全是同一节内容"的问题。代码里还自己做了向量归一化和余弦相似度（`rag_store.py:41-49`）。

---

### 改进 5：证据预算控制 —— 给每次检索设"字数上限"

**做法**：从三处硬性限制进入上下文的证据量：

- 每个片段截断到 **600 字**（`app.py:65`、`agent_setup.py:40`）；
- 摘要预检索每个查询只取 **2 条**（`per_query=2`，`app.py:52`），4 个查询最多 8 条；
- 摘要用**固定的 4 个宽泛查询**覆盖章节，而不是让模型自由发挥查很多次。

**思路**：RAG 的精髓是"给够、但不贪多"。片段太多会稀释关键信息、推高成本，还诱导模型"照着抄"。

---

### 改进 6：recursion_limit = 15

Agent 工具循环理论上可能"调了又调"停不下来。设置 LangGraph 递归上限（`app.py:170`）作为**保险丝**，超过 15 步强制终止，避免极端情况下无限烧 token。

---

### 改进 7：模型从 plus 换成 flash

`config.py:11` 把 `qwen3.7-plus` 换成 `qwen3.7-flash-...`。flash 是轻量版，**单价更低、响应更快**，适合这种"基于给定证据做总结/问答"的任务（推理难度不高，瓶颈在检索质量而非模型智商）。路由分类这种小任务用 flash 更是绰绰有余。

> 选型逻辑：**任务难度匹配模型规格**，不是越贵越好。

---

### 改进 8：embedding 分批 + 错误透出（同期健壮性修复）

论文切成几十个 chunk，一次性提交给 DashScope embedding 接口会因**单批上限 20 条**直接 400 报错。修复（`rag_store.py:20-38`）：

- 按 20 条分批请求；
- 按响应里的 `index` 字段排序，保证向量和输入文本顺序对齐；
- 失败时把 HTTP 响应体一起抛出，方便定位 4xx 根因（而不是吞掉错误）。

---

### 改进 9：重新上传先删旧向量集合

早期一个会话反复上传不同论文，chunk 会混在同一个 ChromaDB collection 里，检索时新旧论文互相污染。现在上传新论文前先 `rag.delete_collection(thread_id)`（`app.py:139`），明确"一个会话一篇论文"的 V1 语义。

---

### 改进 10：SSE 检索进度提示

检索 + 生成要等几秒，早期界面像卡死。现在检测到工具调用/工具返回时，通过 SSE 推送"正在检索论文内容...""检索完成，正在生成回答..."（`app.py:197-207`），让等待过程可见。**不省 token，但大幅改善体感。**

---

### 改进 11：证据铁律与三类分层（可信度主线，非 token 问题）

这部分解决的不是成本，而是**幻觉/可信度**（项目的核心卖点）：

- **证据铁律**（`prompts.py:8-15`）：只有检索到的论文文本是事实来源；一级事实（贡献/方法/实验数据/结论）必须附【来源章节】+【原文引用】；论文没写的必须说"论文中未找到"。
- **三类内容分层**（`prompts.py:17-23`）：问"论文不足/改进 idea"时强制区分——A1 论文明确说的、A2 从论文设计推导的、B 领域背景（标"非论文内容"）、C 我的推断。**严禁把模型自己的知识伪装成论文原文**（比如把 Pre-LN、FlashAttention 这些论文之后的技术说成论文贡献）。

路由分类（改进 1）和这里的 A1/A2/B/C 是**同一套标签体系**：路由用它决定"查不查、查什么"，prompt 用它约束"怎么标注来源"。

---

## 四、前后对比

| 维度 | 早期版本 | 优化后 |
|------|---------|--------|
| **单轮 token** | Agent 多轮工具循环，检索结果在循环内反复重发 | 后端预检索一次，证据注入，单轮生成 |
| **多轮 token** | 随对话轮数**线性膨胀**（旧 ToolMessage 全部重发） | **基本恒定**（历史工具消息每轮清除） |
| **领域类问题（B）** | 照样检索论文、白烧 token | 路由识别后**零检索**，直接用模型知识 |
| **检索内容** | top-k 可能重复同一章节 | MMR 去重 + chunk 记忆，内容不重复 |
| **模型成本** | plus（贵） | flash（便宜快），任务难度匹配 |
| **延迟/体感** | 多轮工具往返，界面无反应 | 单轮生成 + SSE 进度提示 |
| **稳定性** | embedding 超批 400、工具可能死循环 | 分批 + recursion_limit 保险丝 |
| **可信度** | 模型容易混入后续技术、无溯源 | 证据铁律 + 三类分层 + 章节页码引用 |

**一句话**：token 从"随轮数滚雪球"变成"每轮一个固定大小的证据包"，同时回答还更可信了。

---

## 五、还没解决的问题（待办）

1. **没有 cross-encoder 重排**：现在靠向量相似度 + MMR，召回的片段相关性没有经过"精排模型"二次打分。复杂问题可能召回不够准。（V2 可加 reranker）
2. **页码是 PDF 物理页码**：逐行记录的是 PDF 页序，带封面、目录的期刊论文，正文页码会和 PDF 页序有偏移。
3. **只支持单篇论文**：一个会话一篇。多论文库、跨论文对比是 V2（可用 LangGraph 编排 Planner→Retriever→Generator→Validator）。
4. **ChromaDB 距离度量**：集合用的是 Chroma 默认的 L2，而代码里 MMR 自己做了余弦相似度。严格来说建集合时应显式声明 `hnsw:space: cosine`，否则"向量库检索排序"和"MMR 重排"用的度量不一致（这是个已知的坑）。
5. **路由多一跳 LLM 调用**：虽然便宜（50 token），但每个问题都多一次网络往返和一个潜在失败点。目前用"失败回退 A1"兜底，分类错成 B（该查却没查）会降低回答质量——好在回退策略偏保守。
6. **清历史后的长程记忆**：工具证据不跨轮保留，模型对"很早以前问过的论文细节"的记忆，依赖下一轮重新检索；多轮深度追问时连贯性靠保留的问答文字维持，不是完美方案。
7. **OSS 的必要性存疑**：原始 PDF 从不进 LLM，OSS 多一跳延迟；当前保留是为 V2 多论文库持久化原文铺路（`oss_sts.py` 里也留了这条疑问）。

---

## 六、可迁移的方法论（学到这几条就值了）

1. **先定位 token 花在哪，再动手**：本案例的主因不是"模型话多"，而是"检索结果跨轮次重发"。不改根因（历史消息），只在表面限制字数收效甚微。
2. **确定性的事别交给 Agent 循环**：摘要要查哪几节是固定的，后端直接查好注入，比让 Agent 自己反复调工具又快又省。
3. **大对象不进历史，需要时重新获取**：可重新检索的原料（论文片段）用完即清，只保留不可复现的小对象（问答文字）。
4. **用廉价调用做路由决策**：花 50 token 判断"要不要花 2000 token 检索"，是高杠杆的省钱手段。
5. **检索要控量 + 去重 + 保多样性**：截断、限条数、MMR，一个都不能少。
6. **任务难度匹配模型**：flash 够用就别上 plus。
7. **省钱和可信可以一起做**：证据注入既减少了工具循环，又让回答强制溯源——好的架构优化往往一举多得。



## 启动方式

 python -m uvicorn app:app --reload --port 8000  
